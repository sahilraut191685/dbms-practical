Create the following tables. Solve queries by SQL
Deposit (actno,cname,bname,amount,adate)
Branch (bname,city)
Customers (cname, city)
Borrower(loanno,cname,bname, amount)
Add primary key and foreign key wherever applicable.
Insert data into the above created tables.
Solve following queries by SQL
1. Display customer name having living city Bombay and branch city Nagpur
2. Display customer name having same living city as their branch city
3. Display customer name who are borrowers as well as depositors and having living city
Nagpur.
4. Display borrower names having deposit amount greater than 1000 and loan amount greater than
2000
5. Display customer name living in the city where branch of depositor sunil is located.
6. Create an index on deposit table.


```sql id="bank_q1"
-- CREATE TABLES
CREATE TABLE Branch (
    bname VARCHAR(50) PRIMARY KEY,
    city VARCHAR(50)
);

CREATE TABLE Customers (
    cname VARCHAR(50) PRIMARY KEY,
    city VARCHAR(50)
);

CREATE TABLE Deposit (
    actno INT PRIMARY KEY,
    cname VARCHAR(50),
    bname VARCHAR(50),
    amount INT,
    adate DATE,
    FOREIGN KEY (cname) REFERENCES Customers(cname),
    FOREIGN KEY (bname) REFERENCES Branch(bname)
);

CREATE TABLE Borrower (
    loanno INT PRIMARY KEY,
    cname VARCHAR(50),
    bname VARCHAR(50),
    amount INT,
    FOREIGN KEY (cname) REFERENCES Customers(cname),
    FOREIGN KEY (bname) REFERENCES Branch(bname)
);

-- INSERT DATA INTO BRANCH
INSERT INTO Branch VALUES ('SBI_Nagpur', 'Nagpur');
INSERT INTO Branch VALUES ('SBI_Pune', 'Pune');
INSERT INTO Branch VALUES ('SBI_Mumbai', 'Bombay');
INSERT INTO Branch VALUES ('SBI_Delhi', 'Delhi');

-- INSERT DATA INTO CUSTOMERS
INSERT INTO Customers VALUES ('Sunil', 'Bombay');
INSERT INTO Customers VALUES ('Amit', 'Nagpur');
INSERT INTO Customers VALUES ('Sahil', 'Pune');
INSERT INTO Customers VALUES ('Rahul', 'Delhi');
INSERT INTO Customers VALUES ('Priya', 'Nagpur');
INSERT INTO Customers VALUES ('Neha', 'Bombay');

-- INSERT DATA INTO DEPOSIT
INSERT INTO Deposit VALUES (101, 'Sunil', 'SBI_Nagpur', 5000, '2024-01-01');
INSERT INTO Deposit VALUES (102, 'Amit', 'SBI_Nagpur', 2000, '2024-01-02');
INSERT INTO Deposit VALUES (103, 'Sahil', 'SBI_Pune', 1500, '2024-01-03');
INSERT INTO Deposit VALUES (104, 'Rahul', 'SBI_Delhi', 3000, '2024-01-04');
INSERT INTO Deposit VALUES (105, 'Priya', 'SBI_Nagpur', 800, '2024-01-05');
INSERT INTO Deposit VALUES (106, 'Neha', 'SBI_Mumbai', 2500, '2024-01-06');

-- INSERT DATA INTO BORROWER
INSERT INTO Borrower VALUES (201, 'Sunil', 'SBI_Nagpur', 4000);
INSERT INTO Borrower VALUES (202, 'Amit', 'SBI_Nagpur', 3000);
INSERT INTO Borrower VALUES (203, 'Priya', 'SBI_Nagpur', 1000);
INSERT INTO Borrower VALUES (204, 'Rahul', 'SBI_Delhi', 5000);
INSERT INTO Borrower VALUES (205, 'Neha', 'SBI_Mumbai', 2500);

-- 1. CUSTOMER NAME HAVING CITY BOMBAY AND BRANCH CITY NAGPUR
SELECT DISTINCT d.cname
FROM Deposit d
JOIN Customers c ON d.cname = c.cname
JOIN Branch b ON d.bname = b.bname
WHERE c.city = 'Bombay' AND b.city = 'Nagpur';

-- 2. CUSTOMER NAME HAVING SAME LIVING CITY AS THEIR BRANCH CITY
SELECT DISTINCT d.cname
FROM Deposit d
JOIN Customers c ON d.cname = c.cname
JOIN Branch b ON d.bname = b.bname
WHERE c.city = b.city;

-- 3. BORROWERS AS WELL AS DEPOSITORS LIVING IN NAGPUR
SELECT DISTINCT c.cname
FROM Customers c
JOIN Deposit d ON c.cname = d.cname
JOIN Borrower b ON c.cname = b.cname
WHERE c.city = 'Nagpur';

-- 4. BORROWERS HAVING DEPOSIT >1000 AND LOAN >2000
SELECT DISTINCT b.cname
FROM Borrower b
JOIN Deposit d ON b.cname = d.cname
WHERE d.amount > 1000 AND b.amount > 2000;

-- 5. CUSTOMER LIVING IN CITY WHERE SUNIL'S BRANCH IS LOCATED
SELECT cname
FROM Customers
WHERE city = (
    SELECT city
    FROM Branch
    WHERE bname = (
        SELECT bname
        FROM Deposit
        WHERE cname = 'Sunil'
    )
);

-- 6. CREATE INDEX ON DEPOSIT TABLE
CREATE INDEX idx_deposit_cname
ON Deposit(cname);
```
